<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AluraViagens" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary" type="color">@android:color/holo_purple</item>
        <item name="colorPrimaryVariant" type="color">@android:color/holo_purple</item>
        <item name="colorOnPrimary" type="color">@android:color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondaryVariant" type="color">@android:color/holo_purple</item>
        <item name="colorOnSecondary" type="color">@android:color/black</item>
        <!-- Status bar color. -->
        <!-- Customize your theme here. -->
    </style>

</resources>